{
  "name": "witness-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "dev": "pnpm --filter @witness/pwa dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}

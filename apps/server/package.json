{
  "name": "@witness/server",
  "version": "0.1.0",
  "private": true,
  "description": "Witness ingestion server — tus resumable evidence uploads, LoRa HashReceipt ingestion, and receipt/upload correlation (architecture §7)",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@tus/file-store": "^1.5.0",
    "@tus/server": "^1.10.0",
    "@witness/lora-dtn": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
